const base = 5;
let height = 8;

const area = base * height;
console.log(area);

// Formula perimetro P = b + b + h + h ou P = 2(b + h)

// P = 2(b + h)
const perimeter = 2 * (base + height);
console.log(perimeter);

// P = b + b + h + h
const perimeterTwo = base + base + height + height;
console.log(perimeterTwo);